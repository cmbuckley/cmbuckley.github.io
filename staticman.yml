comments:
  allowedFields:
    - name
    - email
    - comment
  requiredFields:
    - name
    - email
    - comment
  branch: master
  format: yaml
  commitMessage: "Add comment to {options.slug}"
  filename: "{@id}"
  path: "_data/comments/{options.slug}"
